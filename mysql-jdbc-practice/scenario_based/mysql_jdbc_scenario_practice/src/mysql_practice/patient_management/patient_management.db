PATIENT_MANAGEMENT
===========================

> [[ patient_info ]] (table with patient information)

Create Table patient_info(
	id int Primary key auto_increment,
    name varchar(50),
    dobb date,
    phone varchar(10) unique,
    email varchar(50) unique,
    address varchar(100),
    blood_group varchar(10)
);

=================================

> [[doctors]] (table with doctor information)

create table doctors(
	id int primary key auto_increment,
    name varchar(50),
    email varchar(50) unique,
    phone varchar(10) unique,
    sepcialty varchar(30) 
);

=================================

> [[appointments]] (table with appointment information)

CREATE TABLE appointments(
	appointment_id INT PRIMARY KEY AUTO_INCREMENT,
   patient_id INT ,
    doctor_id INT,
    appointment_date DATE,
    appoitment_time TIME,
    status ENUM("completed","pending","cancelled"),
    CONSTRAINT patient_fk FOREIGN KEY (patient_id) REFERENCES patient_info(id),
    CONSTRAINT doctor_fk FOREIGN KEY (doctor_id) REFERENCES doctors(id)
  
);

=================================

> [[visits]] (table with visit information)

CREATE TABLE visits (
    id INT PRIMARY KEY AUTO_INCREMENT,
    appointment_id INT NOT NULL,
    diagnosis VARCHAR(100) NOT NULL,
    notes TEXT,
    visit DATE,
    CONSTRAINT fk FOREIGN KEY (appointment_id) REFERENCES appointments(appointment_id)
);


=================================


UC 1.1> [[Inserting patient data]] 

INSERT INTO patient_info(name,dobb,phone,email,address,blood_group)
values("Aman","2002-06-21","1243455434","hello@example.com","12 Anand Nagar,Bhopal, MP, INDIA","A+");

==================================

> [[Inserting doctor data]]
INSERT INTO doctors(name,email,phone,sepcialty)
values("Dr. Smith"," dkjsd@gmail.com","1234567890","Cardiology");
==================================

> [[Inserting appointment data]]
INSERT INTO appointments(patient_id, doctor_id, appointment_date, appoitment_time, status)
values(1,1,"2024-07-01","10:00:00","pending");
==================================

> [[Inserting visit data]]
INSERT INTO visits(appointment_id, diagnosis, notes, visit)
values(1,"Flu","Patient is showing symptoms of flu. Prescribed rest and hydration
.", "2024-07-01");
=================================


UC 1.2> [[Updating patient and showing]]

SELECT * from patient_info where phone="1243455434";
UPDATE  patient_info set blood_group="AB+" where phone ="1243455434";

=====================================

UC 1.3> [[Searching by name, id or phone number]]

SELECT *
FROM patient_info
WHERE name LIKE '%john%';


SELECT * FROM patient_info
WHERE id=1;


SELECT * FROM patient_info
WHERE phone="1243455434";
=====================================


UC 1.4> [[Selecting records based on appointment status and date]]

select a.patient_id,a.appointment_date, a.status,v.diagnosis
FROM appointments a
join visits v ON a.appointment_id=v.appointment_id
where a.patient_id=1;

===================================


